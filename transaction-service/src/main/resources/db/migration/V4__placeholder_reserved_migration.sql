-- V4: PLACEHOLDER — reserved migration slot (intentionally skipped in original sequence).
-- Do NOT remove. Flyway requires sequential versions; this placeholder prevents
-- version-gap failures on fresh deployments.
-- Original migrations jumped from V3 to V6; V4 and V5 are placeholders to maintain continuity.
SELECT 1;
