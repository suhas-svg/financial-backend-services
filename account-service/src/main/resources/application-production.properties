integration.notification.provider=${NOTIFICATION_PROVIDER}
integration.notification.contract-id=${NOTIFICATION_PROVIDER_CONTRACT_ID}
integration.notification.enabled=true
integration.iam.issuer=${OPERATOR_IDP_ISSUER}
integration.iam.audience=${OPERATOR_IDP_AUDIENCE}
integration.iam.role-mappings=${OPERATOR_IDP_ROLE_MAPPINGS}
integration.iam.access-review-reference=${OPERATOR_ACCESS_REVIEW_REFERENCE}
integration.iam.revocation-feed=${OPERATOR_REVOCATION_FEED}
integration.mfa.kms.provider=${MFA_KMS_PROVIDER}
integration.mfa.kms.active-key-id=${MFA_ACTIVE_KEY_ID}
integration.mfa.kms.health-reference=${MFA_KMS_HEALTH_REFERENCE}
